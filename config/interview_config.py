"""
КОНФИГ ДЛЯ ИНТЕРВЬЮ-РЕЖИМА

Интервью-режим — это N мини-сессий подряд по разным темам (без повторов
внутри одного интервью), вместо одной сессии с одной темой, как было
в исходном TheoryTrainer/choose_topic.

Каждая мини-сессия — это полноценная, отдельная TrainingSession
(со своим topic, started_at/finished_at, average_score), сохраняется
в history.json сразу после завершения темы — так же, как раньше
сохранялась единственная сессия в TheoryTrainer.
"""

INTERVIEW_TOPICS_PER_SESSION = 3   # сколько разных тем выбирается на одно интервью
INTERVIEW_QUESTIONS_PER_TOPIC = 2  # сколько вопросов задаётся по каждой теме