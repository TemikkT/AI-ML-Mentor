from langchain_core.prompts import PromptTemplate

# ===================== THEORY MODE =============================

QUESTION_PROMPT = PromptTemplate.from_template( # пропишем структура промпта модели, чтобы не путаться в коде, будем хранить и подавать отдельно
"""
Тема:

{topic}

Заметка пользователя:

{note}

Сгенерируй {num_questions} вопросов.

Требования:

- от простого к сложному
- можно немного дополнять материал
- не выходить далеко за пределы темы
- без больших проектов
- вопросы по ML и аналитике
- НЕ давай задачи на написание кода. Если хочешь спросить про код — попроси пользователя словесно объяснить, как он будет решать задачу, без написания самого кода
"""
)

EVALUATION_PROMPT = PromptTemplate.from_template( # структуры промпта для проверки ответа и отдачи от модели
"""
Ты опытный преподаватель Machine Learning.

Тема:
{topic}

Вопрос:
{question}

Ответ пользователя:
{answer}

Оцени ответ пользователя.

Правила:

- Не цепляйся за грамматические ошибки ответа или немного перефразированную терминологию, только если это правда создаёт путаницу с другими терминами
- Оценивай ответ ТОЛЬКО относительно того, что явно спрошено в вопросе. Не снижай баллы за аспекты, которые не были запрошены
- Если пользователь не упомянул деталь, которая не была явно запрошена в вопросе — это не ошибка, а дополнительный контекст. Не штрафуй за это
- оцени по шкале от 1 до 10
- выдели сильные стороны ответа
- укажи ошибки и пробелы
- дай краткую рекомендацию

Будь справедливым и объективным.
"""
)



# ===================== elo MODE PRACTICE =============================
# Требует явный параметр difficulty (easy/medium/hard) — сложность
# определяется текущим рейтингом темы в PracticeEloProgressManager,
# а не свободной инструкцией "от простого к сложному".

PRACTICE_QUESTION_PROMPT = PromptTemplate.from_template( # генерация практической задачи с кодом фиксированной сложности
"""
Тема:
{topic}

Заметка пользователя:
{note}


Сгенерируй {num_questions} вопросов уровня сложности: {difficulty}.

Требования к сложности:

- задача должна явно требовать написания кода, а не теоретического объяснения
- если тема предполагает выполнение кода (Python, Pandas), явно укажи в условии задачи,
  что итоговый результат нужно вывести через print()
- без больших проектов, задачи по типу литкода
- если сложность easy — Простая задача из самых базовых навыков, решается в парочку строк, а порой и в одну
- если сложность medium — вопрос уже большего характера, к примеру решить небольшую задачу на реальных данных которых ты подашь, решается сложнее и требует больших навыков понимания
- если сложность hard — вопрос должен требовать глубокого понимания, разбора нюансов,
  пограничных случаев или связи с другими темами. Подача реальных кейсов задач на решение.

- НЕ генерируй вопрос смешанной или произвольной сложности — строго придерживайся уровня {difficulty}

Общие требования:

- проверять, что ожидаемый результат реален при выполнении указанных шагов.
- Задачи рода Литкод, на логику и понимание материала.
- можно дополнять материал
- не выходить далеко за пределы темы
- без больших проектов
- вопросы по ML и аналитике
"""
)


PRACTICE_EVALUATION_PROMPT = PromptTemplate.from_template( # оценка кода с учётом результата выполнения (если оно было)

"""
Ты опытный преподаватель программирования и анализа данных.

Тема:
{topic}

Задача:
{question}

Код пользователя:
{answer}

Результат выполнения кода:
{execution_result}

Оцени решение пользователя.

Правила:

- если результат выполнения отсутствует (например, тема не предполагает выполнение кода),
  оценивай код и логику как текст, без привязки к выполнению

- если результат выполнения присутствует и код выполнился с ошибкой (исключение,
  синтаксическая ошибка, неверный вывод) — это СУЩЕСТВЕННЫЙ недостаток решения, а не
  мелкая придирка. Задача тренажёра — писать рабочий код, а не код, который "выглядит
  правильным по логике". Не занижай вес этой ошибки только потому, что причина опечатки
  тривиальна (забытый импорт, лишняя скобка, отступ) — для пользователя важно научиться
  не допускать именно такие ошибки, так как они типичны и в реальной работе

- если результат выполнения присутствует и код выполнился успешно — проверь, что вывод
  (stdout) действительно соответствует тому, что требовала задача

- не требуй единственного "правильного" способа решения — оценивай по сути, а не по форме,
  если разный код приводит к одинаково верному и реально работающему результату — это нормально

- оцени по шкале от 1 до 10
- выдели сильные стороны решения
- укажи ошибки и пробелы (включая ошибки выполнения, если они есть)
- дай краткую рекомендацию

Будь строгим, но справедливым.

"""

)

# ===================== elo MODE Theory =============================

ELO_QUESTION_PROMPT = PromptTemplate.from_template( # генерация вопроса фиксированной сложности для Elo-режима, с учётом слабых мест

"""
Тема:
{topic}

Заметка пользователя:
{note}


Сгенерируй {num_questions} вопросов уровня сложности: {difficulty}.

Требования к сложности:

- если сложность easy — вопрос должен проверять базовое определение или простое применение понятия
- если сложность medium — вопрос должен требовать сравнения понятий, объяснения механизма или применения к небольшому примеру
- если сложность hard — вопрос должен требовать глубокого понимания, разбора нюансов,
  пограничных случаев или связи с другими темами

- НЕ генерируй вопрос смешанной или произвольной сложности — строго придерживайся уровня {difficulty}

{asked_questions}
{weak_points}
Общие требования:

- можно дополнять материал
- не выходить далеко за пределы темы
- без больших проектов
- вопросы по ML и аналитике
- НЕ давай задачи на написание кода. Если хочешь спросить про код — попроси пользователя словесно объяснить, как он будет решать задачу, без написания самого кода
"""
)

# ===================== ЧАТ-ПОМОЩНИК ВО ВРЕМЯ СЕССИИ =============================
# Системный промпт строится Python-кодом (не PromptTemplate.from_template),
# так как он подставляется один раз при старте чата, а не на каждый вызов —
# используем обычную f-строку в месте создания чата (см. chat_helper.py).

CHAT_HELPER_SYSTEM_PROMPT_TEMPLATE = """
Ты — ассистент-наставник, помогающий пользователю разобраться с вопросом
во время тренировки по теме Machine Learning / Python / анализу данных.

ТЕКУЩИЙ вопрос/задача, с которым пользователь работает прямо сейчас:
{question}

Важно: в истории диалога ниже могут быть сообщения, относящиеся к
ПРЕДЫДУЩИМ вопросам той же тренировочной сессии (помощник сопровождает
всю сессию, а не один вопрос). Ориентируйся на ТЕКУЩИЙ вопрос, указанный
выше, а не на то, что обсуждалось раньше — если пользователь спрашивает
про текущий вопрос, отвечай в его рамках, даже если до этого речь шла
о другом вопросе той же темы.

Правила общения:

- Отвечай СТРОГО в рамках темы текущего вопроса. Если пользователь уходит
  в сторону (спрашивает что-то совсем не связанное) — мягко верни его
  к текущему вопросу, не отвечай на посторонние темы.
- НЕ давай готовый, финальный ответ на вопрос сразу. Твоя задача —
  подвести пользователя к ответу самостоятельно: задавай наводящие
  вопросы, объясняй смежные понятия, разбирай вопрос на более простые
  части.
- Если пользователь явно показывает, что совсем не понимает тему
  (несколько неудачных попыток, прямо пишет "я не знаю, объясни"),
  можно раскрыть ЧАСТЬ ответа — но не финальную формулировку целиком,
  а один фрагмент с подробным объяснением, ПОЧЕМУ это так. Дай пользователю
  возможность самому сделать последний шаг к полному ответу.
- Не повторяй и не пересказывай сам вопрос — пользователь его уже видит.
- Будь краток и конкретен, избегай длинных вступлений и общих фраз.
"""


# ========================== EXAM MODE ==========================

EXAM_PROMPT = PromptTemplate.from_template(

"""
Тема:
{topic}

Заметка пользователя:
{note}

Твоя задача — сгенерировать {num_questions} вопросов по теме "{topic}".

Цель:
- Вопросы должны покрыть ~90% всего объёма темы, а не только то, что есть в заметке
- Если заметка неполная или поверхностная — дополняй материал своими знаниями, выходи за её рамки
- Первые вопросы — базовые (определения, простое понимание), затем углубляются, последние — самые сложные (связи между подтемами, нюансы, пограничные случаи)
- Вопросы не должны повторять друг друга — каждый проверяет новый аспект темы
- В сумме вопросы должны дать пользователю полную картину темы, как будто он сдаёт экзамен

Стиль вопросов:
- Чёткие, конкретные формулировки без воды
- В вопросах могут быть мини-кейсы, примеры из практики, сравнения подходов
- Избегай общих вопросов вроде "расскажи всё о ..." — разбивай на конкретные подвопросы

Формат:
- Верни ТОЛЬКО список вопросов, каждый с новой строки
- Без нумерации, без пояснений, без вступлений
- НЕ давай задачи на написание кода. Если хочешь спросить про код — попроси пользователя словесно объяснить, как он будет решать задачу, без написания самого кода
"""
)